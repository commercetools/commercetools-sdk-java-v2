
package com.commercetools.api.predicates.query.t;

//public class PredicateBuilder<T extends Predicate> implements Builder<T> {
////    private final BiFunction<Predicate, PredicateBuilder<Predicate>, T> buildFn;
////
////    private final Predicate predicate;
////
////    public PredicateBuilder(BiFunction<Predicate, PredicateBuilder<Predicate>, T> buildFn) {
////        this.buildFn = buildFn;
////        this.predicate = null;
////    }
////
////    public PredicateBuilder(BiFunction<Predicate, PredicateBuilder<Predicate>, T> buildFn, Predicate predicate) {
////        this.buildFn = buildFn;
////        this.predicate = predicate;
////    }
////
////    public PredicateBuilder<T> predicate(Predicate predicate) {
////        return new PredicateBuilder<>(buildFn, predicate);
////    }
////
////    public PredicateBuilder<T> buildFn(BiFunction<Predicate, PredicateBuilder<Predicate>, T> buildFn) {
////        return new PredicateBuilder<>(buildFn, predicate);
////    }
////
////    @Override
////    public T build() {
////        return buildFn.apply(predicate);
////    }
//}
